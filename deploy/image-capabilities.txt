# Image capabilities trusted by the production deployment contract.
# A capability is emitted as enabled only for the current main commit after CI.
AFFILIATE_REFUND_REVERSAL=1
# Migration 197 is still in its legacy-writer-compatible expand phase. Change
# this to 1 only in the later contract release that removes the DB bridge.
PAYMENT_REVERSAL_COMPONENTS=0
