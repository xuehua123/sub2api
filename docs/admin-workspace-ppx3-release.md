# 0.2.7-ppx.3 管理页面与用户收支发布

- 恢复模型价格、我的订阅、我的订单、邀请有礼的控制台导航，保留站点色调。
- 上游连接增加筛选、总余额、用量收入/利润、历史每日成本及观测倍率；新增分组总览、标签、收藏。
- 支付概览新增独立的用量收入、成本、利润日趋势，不与支付到账额混用。
- 新增仅管理员可见的用户收支排行；充值、退款、成本、利润使用人民币，消耗与余额保留平台额度。
- 默认按期间有消耗用户排序，保留全部历史使用者筛选；修复回退余额归类、退款证据与旧订阅订单关联。
- 逐日人民币汇率必须有来源；缺失数据标记待核对，固定汇率模式明确标记估算，不自动填充生产历史汇率。
- 修正 SSE 心跳测试的计时窗口，覆盖第二次 ticker，不改变运行时网关行为。

## 迁移与回滚

240 新增 upstream_group_annotations，241 新增 user_business_daily_fx；仅加新表/索引，无历史扫描或数据回填，无旧字段修改，兼容旧实例同时运行。
应用回滚可切回原 0.2.7-ppx.2 镜像，保留新增表；不得删除生产数据或改动数据库角色。

## 发布流程

CI 构建不可变摘要镜像；稳如狗先发布并验证，随后加拿大蓝绿切流和连接排空。法国仅缓存镜像，保持副本和应用停止；美国仅外部验证中转，不部署。
按用户指示不增加固定观察等待，保留版本、健康、迁移、真实业务只读验证及回滚快照。
